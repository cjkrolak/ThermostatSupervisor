[flake8]
max-line-length = 80
# known-modules used by flake8-requirements plugin
# this content could be moved to a .flake8 module if needed
# https://pypi.org/project/flake8-requirements/
# dns is a built-in pkg that comes up as a warning so it is included in the list below
known-modules = :[dns,emulator,email_notification,emulator_config,honeywell,honeywell_config,kumocloud,kumocloud_config,kumolocal,kumolocal_config,mmm,mmm_config,sht31,sht31_config,supervise,supervisor_flask_server,thermostat_api,thermostat_common,unit_test_common,utilities]
